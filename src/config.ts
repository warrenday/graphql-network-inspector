export const config = {
  // The maximum request response size in megabytes
  // that the app can reasonably handle. For responses
  // larger than this we can instead show user-friendly
  // messages to avoid freezing/crashing the app.
  //
  // Examples of where large requests cause issues could be:
  // parsing/stringifying json
  // rendering formatted code
  // rendering tracing views
  maxUsableResponseSizeMb: 1.5,
}
